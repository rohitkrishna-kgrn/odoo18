<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_task_quick_transfer_wizard_form" model="ir.ui.view">
        <field name="name">task.quick.transfer.wizard.form</field>
        <field name="model">task.quick.transfer.wizard</field>
        <field name="arch" type="xml">
            <form string="Transfer Task">
                <sheet>
                    <group string="Current Assignees">
                        <field name="current_assignee_ids"
                               widget="many2many_tags"
                               readonly="1"
                               nolabel="1"/>
                    </group>

                    <group string="Transfer To">
                        <field name="transfer_to_ids"
                               widget="many2many_tags"
                               options="{'no_create': True, 'no_open': True}"
                               nolabel="1"/>
                    </group>
                </sheet>
                <footer>
                    <button name="action_transfer"
                            string="Transfer"
                            type="object"
                            class="btn-primary"/>
                    <button string="Cancel" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <!-- Server action bound to project.task list view action dropdown -->
    <record id="action_quick_transfer_task_server" model="ir.actions.server">
        <field name="name">Transfer Task</field>
        <field name="model_id" ref="project.model_project_task"/>
        <field name="binding_model_id" ref="project.model_project_task"/>
        <field name="binding_view_types">list</field>
        <field name="state">code</field>
        <field name="code">
action = {
    'type': 'ir.actions.act_window',
    'res_model': 'task.quick.transfer.wizard',
    'view_mode': 'form',
    'target': 'new',
    'context': dict(env.context, active_ids=records.ids),
}
        </field>
    </record>

</odoo>
