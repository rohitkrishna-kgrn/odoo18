<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_invoice_tree_project_closure_selector" model="ir.ui.view">
        <field name="name">account.move.list.project.closure.selector</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_invoice_tree"/>
        <field name="arch" type="xml">
            <field name="status_in_payment" position="after">
                <field name="invoice_type_classification" optional="show" string="Invoice Type"/>
            </field>
        </field>
    </record>
</odoo>
