<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Parent: Web Leads (no action — acts as a folder) -->
    <menuitem
        id="menu_crm_web_leads_root"
        name="Web Leads"
        parent="crm.crm_menu_root"
        sequence="20"
        groups="sales_team.group_sale_manager"/>

    <!-- Sub: Leads list -->
    <menuitem
        id="menu_crm_web_leads"
        name="Leads"
        parent="menu_crm_web_leads_root"
        action="action_crm_web_lead"
        sequence="10"
        groups="sales_team.group_sale_manager"/>

    <!-- Sub: API Info wizard -->
    <menuitem
        id="menu_crm_web_lead_api_info"
        name="API Details"
        parent="menu_crm_web_leads_root"
        action="action_crm_web_lead_api_info"
        sequence="20"
        groups="sales_team.group_sale_manager"/>
</odoo>
