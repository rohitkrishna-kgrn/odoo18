<odoo>
    <!-- Helpdesk stages -->
    <record id="stage_new" model="helpdesk_rk.stage">
        <field name="name">New</field>
        <field name="sequence">10</field>
    </record>
    <record id="stage_in_progress" model="helpdesk_rk.stage">
        <field name="name">In Progress</field>
        <field name="sequence">20</field>
    </record>
    <record id="stage_done" model="helpdesk_rk.stage">
        <field name="name">Done</field>
        <field name="sequence">30</field>
    </record>
    <record id="stage_rejected" model="helpdesk_rk.stage">
        <field name="name">Rejected</field>
        <field name="sequence">40</field>
    </record>

    <!-- Dedicated subtype for the Ticket Chat widget: kept out of the default
         Chatter "Follow" subscription list so chat messages never trigger
         email notifications. -->
    <record id="mt_ticket_chat" model="mail.message.subtype">
        <field name="name">Ticket Chat</field>
        <field name="res_model">helpdesk_rk.ticket</field>
        <field name="default" eval="False"/>
        <field name="internal" eval="True"/>
    </record>
</odoo>